export default function WidgetsIndexPage() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'system-ui, -apple-system, BlinkMacSystemFont, sans-serif',
        color: '#111827'
      }}
    >
      Pulse widgets are served through NitroStack Studio previews.
    </main>
  );
}
